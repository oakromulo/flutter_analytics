name: test_app
description: dummy app for integration tests
version: 5.6.7+8

environment:
  flutter: '>=1.12.13+hotfix.5 <2.0.0'
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_analytics:
    path: ../
  flutter_driver:
    sdk: flutter
  test: any

flutter:
  uses-material-design: true
